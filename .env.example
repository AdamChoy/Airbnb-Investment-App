# Copy to .env (or set as Streamlit Cloud secrets) and fill in real values.
# Never commit the real .env or .streamlit/secrets.toml.

# Optional. Powers the AI-generated investment insight on the Score page
# (streamlit_app/ai_insight.py). If unset, the app falls back to a static
# templated write-up — it will not crash or break the demo.
ANTHROPIC_API_KEY=
